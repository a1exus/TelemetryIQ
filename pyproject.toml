[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rexy"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["gt-telem>=1.2.1"]

[project.scripts]
rexy = "rexy.__main__:main"
