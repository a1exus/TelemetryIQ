# Task: Core feature

**Phase**: 2 — Core features  
**Status**: Not started

## Objective

Implement the first core feature per specs (e.g. F1).

## Acceptance criteria

- [ ] Feature implemented and wired in
- [ ] Matches behavior described in `specs.md`
- [ ] No known regressions

## Notes

- Use `TurismoClient` from gt-telem; consider `run()`, `run_async()`, or callbacks + events (`GameEvents`, `RaceEvents`, `DriverEvents`).
- Duplicate or split this file per feature (e.g. `02-telemetry-dash.md`, `02-recorder.md`).
